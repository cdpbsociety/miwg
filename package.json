{
  "name": "@miwg/codetest",
  "version": "0.1.0",
  "license": "UNLICENSED",
  "private": true,
  "scripts": {
    "start": "mvn spring-boot:run -Dspring-boot.run.profiles=local",
    "build": "mvn clean install -DskipTests",
    "test": "mvn test"
  }
}
